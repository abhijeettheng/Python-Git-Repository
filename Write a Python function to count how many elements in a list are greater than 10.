#Q. Write a Python function to count how many elements in a list are greater than 10.


def count_elements(lst):
    count = 0
    for element in lst:
        if element > 10:
            count += 1
    return count

numbers = []
for i in range(5):
    num = int(input(f"Enter number {i+1}: "))
    numbers.append(num)

print("Count of elements greater than 10:", count_elements(numbers))
