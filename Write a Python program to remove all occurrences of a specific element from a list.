#Q. Write a Python program to remove all occurrences of a specific element from a list.


numbers = []

for i in range(6):
    num = int(input(f"Enter number {i+1}: "))
    numbers.append(num)

target = int(input("Enter the element to remove: "))

result = []
for num in numbers:
    if num != target:
        result.append(num)

print("Original list:", numbers)
print("After removing all occurrences:", result)
