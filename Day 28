#Q1. Write a Python function to return the product of all elements in a list.

"""
def product_elements(lst):
    product = 1
    for element in lst:
        product *= element
    return product

numbers = []

for i in range(5):
    num = int(input(f"Enter number {i+1}: "))
    numbers.append(num)

print("Product of elements:", product_elements(numbers))
"""


#Q2. Write a Python program to check if two strings are anagrams of each other (ignore spaces and case).

"""
def anagrams(string1, string2):

    string1 = string1.replace(" ", "").lower()
    string2 = string2.replace(" ", "").lower()

    # Check if sorted letters are equal
    return sorted(string1) == sorted(string2)

string1 = input("Enter first string: ")
string2 = input("Enter second string: ")

if anagrams(string1, string2):
    print("The strings are anagrams.")
else:
    print("The strings are NOT anagrams.")
"""






#Q3. Write a Python function to remove all vowels from a given string and return the result.


def remove_vowels(string):
    vowels = "aeiouAEIOU"
    result = ""
    for char in string:
        if char not in vowels:
            result += char
    return result

string = input("Enter the string: ")
print(remove_vowels(string))
